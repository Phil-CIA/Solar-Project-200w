# Solar Project New Chat Handoff

## Session Closeout - 2026-08-08

Project: Solar Project 200W
Phase: Rev 1 Wi-Fi coprocessor integration and pre-routing verification

## Current Objective

Close this session with the Wi-Fi ownership and interface architecture captured, then verify the hardware boundary before PCB routing.

## What Was Completed In This Stop Window

1. Accepted the ESP32-C3 development module as a 5 V-powered network coprocessor.
2. Kept the STM32 as the sole owner of charging, PWM, protection, limits, and faults.
3. Accepted UART TX/RX with optional RTS/CTS as the processor interface.
4. Defined the initial framed-UART, Wi-Fi provisioning, MQTT, and OTA boundaries in DEC-015 and docs/wifi-coprocessor-architecture.md.
5. Reviewed the schematic arrangement without making a schematic edit.

## Verified State At Stop

- ESP32 module power is intentionally `CTRL_5V` into its `5V` pin.
- Its exposed `3.3V` pin is unused; U5 separately generates `CTRL_3V3`.
- The checked-in ERC report shows 0 errors and 0 warnings but predates the latest schematic edits.
- D10 direction, the 5 V current budget, antenna keepout, reset/boot control, and C52 margin still require closure.

## Next Session Actions (In Order)

1. Review HANDOFF.md, SOLAR_HANDOFF_2026-08-08.md, and docs/wifi-coprocessor-architecture.md.
2. Capture git status and export fresh ERC from the Rev 1 schematic.
3. Verify D10 polarity and calculate the USB/`CTRL_5V` current budget.
4. Confirm STM32-to-ESP32 UART crossing, GPIO mapping, RTS/CTS, and reset/boot requirements.
5. Add antenna keepout and supply checks to the PCB closure checklist.
6. Define the initial UART packet fields and MQTT telemetry schema.

## Local State At Stop

- Existing uncommitted schematic and handoff edits remain in the working tree.
- No schematic file was changed during this documentation closeout.

## Rules For Next Session

1. Preserve the STM32 safety boundary in DEC-015.
2. Do not make charging depend on ESP32 or network availability.
3. Preserve existing local changes and avoid destructive cleanup.
4. Keep KiCad lock, autosave, and cache artifacts out of commits.

## First Action Next Session

Read HANDOFF.md and SOLAR_HANDOFF_2026-08-08.md, then regenerate ERC before editing schematic content.

## Suggested Prompt For New Chat

Continue in Solar-Project-200w from SOLAR_HANDOFF_2026-08-08.md. Preserve DEC-015: STM32 owns charger safety and the 5 V-powered ESP32-C3 development module is a network coprocessor. Regenerate ERC, verify D10 and the CTRL_5V budget, confirm UART/RTS/CTS/reset mapping and antenna keepout, then define the initial framed-UART and MQTT telemetry schemas.
